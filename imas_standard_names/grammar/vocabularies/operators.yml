# Operator registry vocabulary
#
# All operator tokens are stored as bare tokens
# (no trailing _of_); the rendering engine appends _of_ for unary_prefix
# operators and handles binary separators.
#
# Schema per entry:
#   kind          - unary_prefix | unary_postfix | binary
#   precedence    - int; higher = applied further out (wraps more of the name)
#   returns       - optional str describing the return kind
#   arg_types     - optional list[str] constraining valid input kinds
#   separator     - str used between arguments for binary operators
#   indexed       - bool; if true, index parameters are appended to the name
#   index_params  - list[str] naming the index parameters (requires indexed=true)
#   semantic_effects - stable semantic categories supplied to consumers
#
# Rendering templates:
#   unary_prefix   ->  <token>_of_<inner>
#   unary_postfix  ->  <inner>_<token>
#   binary         ->  <token>_of_<A><separator><B>
# A unary operator on an ordinary base with an ``of``, ``at``, or ``due_to``
# tail renders immediately before that tail. Operators within either operand
# of a binary expression retain the templates above at every nesting depth.
#
# Unified registry of prefix, postfix, and binary operators.

operators:

  # ====================================================================
  # Unary prefix operators (rendered: <token>_of_<inner>)
  # ====================================================================

  accumulated:
    kind: unary_prefix
    precedence: 15
    returns: scalar
    dimension_transforming: true

  cumulative:
    kind: unary_prefix
    precedence: 15
    returns: scalar
    dimension_transforming: true

  cumulative_inside_flux_surface:
    kind: unary_prefix
    precedence: 15
    returns: scalar
    dimension_transforming: true

  derivative:
    kind: unary_prefix
    precedence: 20
    returns: scalar_or_vector
    dimension_transforming: true

  derivative_with_respect_to:
    kind: unary_prefix
    precedence: 20
    returns: scalar_or_vector
    indexed: true
    index_params: [coord]
    dimension_transforming: true

  flux_surface_averaged:
    kind: unary_prefix
    precedence: 30
    returns: scalar_or_vector
    flux_surface_reduction: true

  gradient:
    kind: unary_prefix
    precedence: 20
    returns: vector
    arg_types: [scalar]
    dimension_transforming: true

  inverse:
    kind: unary_prefix
    precedence: 20
    returns: scalar_or_vector
    dimension_transforming: true

  line_averaged:
    kind: unary_prefix
    precedence: 30
    returns: scalar

  line_integrated:
    kind: unary_prefix
    precedence: 30
    returns: scalar
    dimension_transforming: true

  logarithm:
    kind: unary_prefix
    precedence: 20
    returns: scalar
    dimensionless: true

  magnitude:
    kind: unary_postfix
    precedence: 10
    returns: scalar
    arg_types: [vector, complex]

  maximum:
    kind: unary_prefix
    precedence: 30
    returns: scalar_or_vector
    extremum_reduction: true

  maximum_over_flux_surface:
    kind: unary_prefix
    precedence: 30
    returns: scalar_or_vector
    flux_surface_reduction: true

  minimum:
    kind: unary_prefix
    precedence: 30
    returns: scalar_or_vector
    extremum_reduction: true

  minimum_over_flux_surface:
    kind: unary_prefix
    precedence: 30
    returns: scalar_or_vector
    flux_surface_reduction: true

  normalized:
    kind: unary_prefix
    precedence: 15
    returns: scalar_or_vector
    dimensionless: true

  per_toroidal_mode:
    kind: unary_prefix
    precedence: 35
    returns: scalar_or_vector

  per_poloidal_mode:
    kind: unary_prefix
    precedence: 35
    returns: scalar_or_vector

  per_toroidal_and_poloidal_mode_number:
    kind: unary_prefix
    precedence: 35
    returns: scalar_or_vector

  perturbed:
    kind: unary_prefix
    precedence: 15
    returns: scalar_or_vector
    dimensionless: true

  radial_derivative:
    kind: unary_prefix
    precedence: 20
    returns: scalar_or_vector
    dimension_transforming: true

  root_mean_square:
    kind: unary_prefix
    precedence: 30
    returns: scalar

  second_derivative:
    kind: unary_prefix
    precedence: 20
    returns: scalar_or_vector
    dimension_transforming: true

  second_radial_derivative:
    kind: unary_prefix
    precedence: 20
    returns: scalar_or_vector
    dimension_transforming: true

  square:
    kind: unary_prefix
    precedence: 20
    returns: scalar_or_vector
    dimension_transforming: true

  surface_integrated:
    kind: unary_prefix
    precedence: 30
    returns: scalar
    dimension_transforming: true

  # Finite/discrete increment (Delta) of the base over an event, not a rate.
  # Unlike time_derivative (which returns a per-time rate and renders
  # with _of_), change_in preserves the base unit and renders BARE
  # (change_in_electron_density). It therefore also appears in the
  # _BARE_PREFIX_TRANSFORMATIONS set in model.py. Suits discrete event-driven
  # changes (sawtooth, ELM, pellet) under core_instant_changes/.
  change_in:
    kind: unary_prefix
    precedence: 20
    returns: scalar_or_vector
    semantic_effects: [temporal_change]

  time_averaged:
    kind: unary_prefix
    precedence: 30
    returns: scalar_or_vector

  time_derivative:
    kind: unary_prefix
    precedence: 20
    returns: rate
    dimension_transforming: true
    semantic_effects: [temporal_change]

  variation:
    kind: unary_prefix
    precedence: 20
    returns: scalar_or_vector

  volume_averaged:
    kind: unary_prefix
    precedence: 30
    returns: scalar_or_vector

  volume_integrated:
    kind: unary_prefix
    precedence: 30
    returns: scalar
    dimension_transforming: true

  # --- Uncertainty modifier operators (error siblings) ---
  # Three deterministic operators for IMAS _error_upper / _error_lower /
  # _error_index companion fields.  Applied as outer wrappers of the
  # parent standard name: upper_uncertainty_of_<P>, etc.

  upper_uncertainty:
    kind: unary_prefix
    precedence: 5
    returns: scalar_or_vector

  lower_uncertainty:
    kind: unary_prefix
    precedence: 5
    returns: scalar_or_vector

  uncertainty_index:
    kind: unary_prefix
    precedence: 5
    returns: scalar

  # ====================================================================
  # Unary postfix operators (rendered: <inner>_<token>)
  # ====================================================================

  # Scalar-extraction operators.  Postfix like magnitude so they compose
  # with a projection: radial_electric_field_amplitude, not
  # amplitude_of_radial_electric_field (prefix + component are mutually
  # exclusive in the flat model).
  amplitude:
    kind: unary_postfix
    precedence: 10
    returns: scalar

  real_part:
    kind: unary_postfix
    precedence: 10
    returns: scalar
    arg_types: [complex]

  imaginary_part:
    kind: unary_postfix
    precedence: 10
    returns: scalar
    arg_types: [complex]

  bessel_0:
    kind: unary_postfix
    precedence: 10
    returns: scalar
    indexed: true
    index_params: [order]

  bessel_1:
    kind: unary_postfix
    precedence: 10
    returns: scalar
    indexed: true
    index_params: [order]

  constraint_exact_flag:
    kind: unary_postfix
    precedence: 5
    returns: scalar

  fourier_coefficient:
    kind: unary_postfix
    precedence: 40
    indexed: true
    index_params: [m, n]

  gyroaveraged:
    kind: unary_postfix
    precedence: 10
    returns: scalar_or_vector

  moment:
    kind: unary_postfix
    precedence: 10
    returns: scalar

  # Division by the solid angle the quantity is emitted into, adding sr^-1 to
  # the unit (a volumetric source rate m^-3.s^-1 becomes m^-3.s^-1.sr^-1).
  # Postfix so the per-unit normalization reads after the quantity it divides,
  # as it does in physics prose ("source rate per solid angle"), and one token
  # serves every quantity rather than a base per emitted species.
  per_solid_angle:
    kind: unary_postfix
    precedence: 15
    returns: scalar_or_vector
    dimension_transforming: true

  reference_waveform:
    kind: unary_postfix
    precedence: 5
    returns: scalar

  waveform:
    kind: unary_postfix
    precedence: 5
    returns: scalar

  # ====================================================================
  # Binary operators (rendered: <token>_of_<A><separator><B>)
  # ====================================================================

  difference:
    kind: binary
    separator: _and_
    precedence: 5

  product:
    kind: binary
    separator: _and_
    precedence: 5

  ratio:
    kind: binary
    separator: _to_
    precedence: 5
